//! wgpu-based mesh renderer

use wgpu::*;

pub struct ViewportRenderer {
    // TODO: wgpu device, queue, surface, pipelines
}

impl ViewportRenderer {
    pub async fn new(window: &winit::window::Window) -> Self {
        // TODO: Initialize wgpu
        Self {}
    }
}
